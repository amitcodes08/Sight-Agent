{
  "name": "sight-agent",
  "version": "0.1.0",
  "private": true,
  "description": "SightAgent — An open-source visual AI agent Chrome extension",
  "scripts": {
    "dev:extension": "npm run dev -w extension",
    "dev:backend": "npm run dev -w backend",
    "dev": "npm run dev:backend & npm run dev:extension",
    "build:extension": "npm run build -w extension",
    "build:backend": "npm run build -w backend",
    "build": "npm run build:extension && npm run build:backend",
    "lint": "npm run lint -w extension && npm run lint -w backend",
    "clean": "npm run clean -w extension && npm run clean -w backend"
  },
  "workspaces": [
    "extension",
    "backend"
  ],
  "engines": {
    "node": ">=18.0.0",
    "npm": ">=9.0.0"
  }
}
